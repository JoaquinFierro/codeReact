import { DonutDatasetTransition } from "./DonutDatasetTransition";
import React from 'react'

export const DonutDatasetTransitionDemo = ({ width = 700, height = 400 }) => (
  <DonutDatasetTransition width={width} height={height} />
);
